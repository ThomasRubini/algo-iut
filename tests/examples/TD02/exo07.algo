algorithme afficheMinuscule
debut
	declarer Car : caractere;
	Car <- 'z';
	boucle 
		afficher (Car, '/', toupper(Car));
		ligne_suivante;
		si (Car vaut 'a') sortie;
		fsi
		Car <- pred (Car);	
	fboucle
fin	